<%= form_for (@newplayer) do |f| %>
 :
 :
 <%= f.label :画像 %><br />
 <%= f.file_field :img, class: "image" %>
 :
 <%= f.submit %>

<% end %>

          <%= form_for @newplayer, url: newplayers_index_path do |f| %>
            <%= f.file_field :img %>
            <% end %>


                <% if @user.image? %>
      <%= image_tag @user.image.url %>
    <% else %>
      <%= image_tag "noimage.gif" %>
    <% end %>